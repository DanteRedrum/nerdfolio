[control]
nf-ansible-ubuntu ansible_host=127.0.0.1 ansible_connection=local

[windows]
nf-windows-host ansible_host=192.168.100.1

[windows:vars]
ansible_user=ansible
ansible_password= "{{ vault_windows_password }}"
ansible_connection=winrm
ansible_winrm_transport=basic
ansible_winrm_port=5985
ansible_winrm_scheme=http
ansible_become=false

[lab]
# VMs added here as they are provisioned

[security_lab]
# Isolated VMs — provisioned and torn down by playbook
