# ITFlow Environment Variables
# Copy to .env and fill in values — do not commit .env to git

ITFLOW_URL=https://itflow.yourdomain.local
ITFLOW_DOMAIN=itflow.yourdomain.local

DB_NAME=itflow
DB_USER=itflow
DB_ROOT_PASSWORD=changeme_root
DB_PASSWORD=changeme_itflow
