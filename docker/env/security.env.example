# Security stack environment variables
# Copy to security.env and fill in values
# security.env is gitignored — never commit real values

PUID=1000
PGID=1000
TZ=America/Chicago
WAZUH_API_PASSWORD=
OPENVAS_PASSWORD=
